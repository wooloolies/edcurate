{
  "buildCommand": "pip install -e . 2>&1 && echo '=== PIP OK ===' && python -c 'from src.main import app; print(\"IMPORT OK\")' 2>&1 && alembic upgrade head 2>&1",
  "ignoreCommand": "[ \"$VERCEL_ENV\" = \"preview\" ]"
}
